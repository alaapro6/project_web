# Gift Finder Frontend Dockerfile
# المرحلة 1: البناء
FROM node:18-alpine AS builder

WORKDIR /app

# نسخ ملفات package
COPY package*.json ./

# تثبيت المتطلبات
RUN npm ci

# نسخ كود المصدر
COPY . .

# بناء التطبيق
RUN npm run build

# المرحلة 2: الإنتاج
FROM nginx:alpine

# نسخ الملفات المبنية
COPY --from=builder /app/dist /usr/share/nginx/html

# نسخ إعدادات nginx
COPY nginx.conf /etc/nginx/conf.d/default.conf

# فتح المنفذ
EXPOSE 80

CMD ["nginx", "-g", "daemon off;"]
